# FastAPI LibGen Dashboard Environment Configuration

# Database Configuration
DATABASE_URL=sqlite:///./libgen_dashboard.db
# For PostgreSQL: DATABASE_URL=postgresql://user:password@localhost/libgen_dashboard

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production-make-it-long-and-random
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=true

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# LibGen Configuration (inherited from parent project)
LIBGEN_SEARCH_MIRRORS=https://libgen.la,https://libgen.li,https://libgen.gl,https://libgen.vg
LIBGEN_DOWNLOAD_MIRRORS=https://libgen.la,https://libgen.li,https://libgen.gl,https://libgen.vg
LIBGEN_MAX_RESULTS=200
LIBGEN_SEARCH_TIMEOUT=30
LIBGEN_MAX_RETRIES=2
LIBGEN_RESOLVE_FINAL_URLS=true

# Logging
LOG_LEVEL=INFO

# Optional: Redis for caching (if you want to add caching later)
# REDIS_URL=redis://localhost:6379/0

# Optional: Email configuration (for password reset, etc.)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
